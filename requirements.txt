ag2[openai]>=0.11.0
streamlit>=1.33.0
pypdf>=4.2.0
# openai package is pulled in transitively by ag2[openai] and is required
# because the HuggingFace Inference API is consumed via its OpenAI-compatible endpoint.
openai>=1.30.0